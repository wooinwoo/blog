import { ProjectPostLayout } from "@/components/layout/project-post-layout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ProjectPostLayout>{children}</ProjectPostLayout>;
}
